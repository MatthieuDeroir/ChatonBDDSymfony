{% for c in categories %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path("chaton_voir", {"idCategorie":c.id}) }}">{{ c.titre }}</a>
    </li>
{% endfor %}
<li class="nav-item">
    <a class="nav-link" href="{{ path("voir_owners")}}">Propriétaires</a>
</li>